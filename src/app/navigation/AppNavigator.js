import React,{useState} from 'react'; import {StatusBar,View,StyleSheet} from 'react-native'; import {SafeAreaView} from 'react-native'; import TopBar from '../../shared/components/TopBar'; import BottomTabs from '../../shared/components/BottomTabs'; import HomeScreen from '../../features/home/screens/HomeScreen'; import TeachingsScreen from '../../features/teachings/screens/TeachingsScreen'; import LibraryScreen from '../../features/library/screens/LibraryScreen'; import GatheringsScreen from '../../features/gatherings/screens/GatheringsScreen'; import QuestionsScreen from '../../features/questions/screens/QuestionsScreen'; import {COLORS} from '../../shared/theme/colors';
const names={home:'Home',teachings:'Teachings',library:'Library',gatherings:'Gatherings',questions:'Ask a Question'};
export default function AppNavigator(){const [active,setActive]=useState('home'); const content={home:<HomeScreen onNavigate={setActive}/>,teachings:<TeachingsScreen/>,library:<LibraryScreen/>,gatherings:<GatheringsScreen/>,questions:<QuestionsScreen/>}[active]; return <SafeAreaView style={s.safe}><StatusBar barStyle="light-content" backgroundColor={COLORS.bg}/><View style={s.app}><TopBar screenName={names[active]}/><View style={s.body}>{content}</View><BottomTabs active={active} onNavigate={setActive}/></View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:COLORS.black},app:{flex:1,backgroundColor:COLORS.bg},body:{flex:1}});
